{!userTools.loggedIn && (
            <li className="nav-item">
              <Nav.Link
                onClick={() => navigate("/cars")}
                className={`nav-link ${active === "about" ? "fw-bold" : ""}`}
              >
                <FaArrowRight />
              </Nav.Link>
            </li>
          )}